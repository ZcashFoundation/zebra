project: zebra-network
kind: Fixed
body: '`PeerSet` now routes single-hash `BlocksByHash` requests to ready peers that advertised `NODE_NETWORK` before other peers, and delays its synthetic `NotFoundRegistry` error by `INVENTORY_BUSY_PEER_REFUSAL_DELAY` (1.5 seconds) while a busy peer might still have the block. Refusals are counted in the `zcash.net.peer_set.inventory_refusal` metric.'
time: 2026-09-24T00:38:27.544666125Z
