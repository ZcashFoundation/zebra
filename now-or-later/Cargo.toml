[package]
name = "now-or-later"
version = "0.0.1"
authors = ["Zcash Foundation <zebra@zfnd.org>"]
license = "MIT OR Apache-2.0"
edition = "2018"

[dependencies]
pin-project = "1.0.7"

[dev-dependencies]
futures = "0.3.18"
tokio = { version = "1.15.0", features = ["full"] }
