# Release workflow: crate publishing and GitHub Release creation.
#
# Changelogs are authored in source PRs and finalized before release.
#
# Pipeline:
#   push to main                  → creates/updates the single Release PR
#   merged, approved Release PR   → publishes crates + tags and creates the
#                                   app-authored zebrad GitHub Release
#
# Downstream triggers (independent workflows, not managed here):
#   release:released  → release-binaries.yml (Docker Hub)
#   release:published → zfnd-deploy-nodes-gcp.yml (GCP deployment)
name: Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [closed]

# Deny all at workflow level — each job declares minimum permissions
permissions: {}

jobs:
  # ── Stage 1: Create or update the Release PR ────────────────────────────
  #
  # Runs on every push to main. Detects changed crates by comparing local
  # source against crates.io, bumps versions based on conventional commits,
  # and opens or updates a single Release PR.
  release-pr:
    name: Create or update Release PR
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    # Avoid racing updates to the single release-plz Release PR branch when
    # multiple commits land on main while a previous release-pr job is running.
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Generate release app token
        id: app-token
        if: vars.RELEASE_APP_ID != ''
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 #v3.2.0
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: write
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd #v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
          token: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
      - name: Create or update Release PR
        id: release-plz-pr
        uses: release-plz/action@e8792575c7f2366cf6ff3ccc33ead9ace5b691c7 #v0.5.130
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
      - name: Add zebrad version to Release PR title
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
          RELEASE_PRS: ${{ steps.release-plz-pr.outputs.prs }}
        run: |
          set -euo pipefail

          ZEBRAD_RELEASE_PR="$(jq -c '
            map(select(any(.releases[]?; .package_name == "zebrad")))
            | first // empty
          ' <<< "${RELEASE_PRS:-[]}")"

          if [ -z "${ZEBRAD_RELEASE_PR}" ]; then
            echo "No zebrad release in release-plz output; keeping the generated title."
            exit 0
          fi

          PR_NUMBER="$(jq -r '.number' <<< "${ZEBRAD_RELEASE_PR}")"
          VERSION="$(jq -r '
            .releases[] | select(.package_name == "zebrad") | .version
          ' <<< "${ZEBRAD_RELEASE_PR}")"

          if [ -z "${PR_NUMBER}" ] || [ "${PR_NUMBER}" = "null" ] || [ -z "${VERSION}" ] || [ "${VERSION}" = "null" ]; then
            echo "::error::release-plz did not return both the PR number and zebrad version: ${ZEBRAD_RELEASE_PR}"
            exit 1
          fi

          gh pr edit "${PR_NUMBER}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "chore: release v${VERSION}"

  # ── Stage 2: Publish crates and create the public zebrad release ────────
  #
  # Runs only when an approved Release PR from the release-plz branch is
  # merged into main. The merged commit is the immutable release target.
  # The job:
  #   1. Publishes changed crates to crates.io in dependency order
  #   2. Uses trusted publishing (OIDC) — no static API tokens
  #   3. Creates per-crate git tags
  #   4. Creates one public GitHub Release for zebrad
  #
  # The Release PR merge is the human gate. The GitHub Release must be created
  # with a GitHub App installation token because release events created by the
  # repository GITHUB_TOKEN do not trigger downstream workflows.
  #
  release:
    name: Publish crates and GitHub Release
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.merged == true &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      startsWith(github.event.pull_request.head.ref, 'release-plz-') &&
      contains(github.event.pull_request.labels.*.name, 'A-release')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      pull-requests: read
    environment: release
    concurrency:
      group: release-publish
      cancel-in-progress: false
    steps:
      - name: Verify approved Release PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPOSITORY: ${{ github.repository }}
          TARGET_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          set -euo pipefail

          RELEASE_PR="$(gh pr view "${PR_NUMBER}" \
            --repo "${REPOSITORY}" \
            --json baseRefName,headRefName,labels,mergeCommit,reviewDecision,state)"

          if ! jq -e --arg target_sha "${TARGET_SHA}" '
            .state == "MERGED" and
            .reviewDecision == "APPROVED" and
            .baseRefName == "main" and
            (.headRefName | startswith("release-plz-")) and
            .mergeCommit.oid == $target_sha and
            any(.labels[]; .name == "A-release")
          ' <<< "${RELEASE_PR}" >/dev/null; then
            echo "::error::PR #${PR_NUMBER} is not an approved A-release Release PR merged into main at ${TARGET_SHA}."
            exit 1
          fi
      - name: Generate release app token
        id: app-token
        if: vars.RELEASE_APP_ID != ''
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 #v3.2.0
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write
          permission-pull-requests: read
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd #v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.merge_commit_sha }}
          token: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
      - name: Restore main branch context
        env:
          TARGET_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          git switch --force-create main "${TARGET_SHA}"
          git branch --set-upstream-to=origin/main main
      - name: Validate release app token
        if: steps.app-token.outputs.token == ''
        run: |
          echo "::error::Configure RELEASE_APP_ID and RELEASE_APP_PRIVATE_KEY before merging a Release PR. The app token is required before crates publish so the zebrad GitHub Release can trigger Docker and GCP workflows."
          exit 1
      - name: Validate release changelogs
        id: notes
        env:
          TARGET_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        run: |
          set -euo pipefail

          notes_file="${RUNNER_TEMP}/release-notes.md"
          expected_releases="${RUNNER_TEMP}/expected-releases.tsv"
          # Zebra allows only squash merges, so the parent is main immediately before this Release PR.
          .github/scripts/validate-release-changelogs.sh \
            "${TARGET_SHA}^" \
            "${TARGET_SHA}" \
            "${notes_file}" \
            "${expected_releases}"

          if [ ! -s "${notes_file}" ]; then
            echo "required=false" >> "${GITHUB_OUTPUT}"
            echo "This Release PR does not publish zebrad; skipping GitHub Release notes."
            exit 0
          fi

          VERSION="$(awk '
            /^\[package\]$/ {
              in_package = 1
              next
            }
            /^\[/ {
              in_package = 0
            }
            in_package && /^[[:space:]]*version[[:space:]]*=/ {
              sub(/^[^=]*=[[:space:]]*"/, "")
              sub(/".*$/, "")
              print
              exit
            }
          ' zebrad/Cargo.toml)"

          if [ -z "${VERSION}" ]; then
            echo "::error::Could not read the zebrad package version from zebrad/Cargo.toml."
            exit 1
          fi

          delim="SECTION_$(openssl rand -hex 8)"
          {
            echo "required=true"
            echo "version=${VERSION}"
            echo "tag=v${VERSION}"
            echo "section<<${delim}"
            cat "${notes_file}"
            echo "${delim}"
          } >> "${GITHUB_OUTPUT}"
      - name: Install libclang for librocksdb-sys bindgen
        run: sudo apt-get update && sudo apt-get install -y libclang-dev
      - name: Validate release packages without publishing
        uses: release-plz/action@e8792575c7f2366cf6ff3ccc33ead9ace5b691c7 #v0.5.130
        with:
          command: release
          dry_run: true
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
      - name: Publish crates and create tags
        id: release-plz
        uses: release-plz/action@e8792575c7f2366cf6ff3ccc33ead9ace5b691c7 #v0.5.130
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
      - name: Verify published package versions
        id: zebrad-release
        env:
          EXPECTED_TAG: ${{ steps.notes.outputs.tag }}
          EXPECTED_VERSION: ${{ steps.notes.outputs.version }}
          NOTES_REQUIRED: ${{ steps.notes.outputs.required }}
          RELEASES: ${{ steps.release-plz.outputs.releases }}
        run: |
          set -euo pipefail

          EXPECTED_RELEASES="$(jq -Rsc '
            split("\n")
            | map(select(length > 0) | split("\t") | {package_name: .[0], version: .[1]})
            | sort_by(.package_name)
          ' "${RUNNER_TEMP}/expected-releases.tsv")"
          ACTUAL_RELEASES="$(jq -c '
            map({
              package_name: (.package_name // .package // .name),
              version: .version
            })
            | sort_by(.package_name)
          ' <<< "${RELEASES:-[]}")"

          if [ "${ACTUAL_RELEASES}" != "${EXPECTED_RELEASES}" ]; then
            echo "::error::release-plz returned ${ACTUAL_RELEASES}, but the validated Release PR contains ${EXPECTED_RELEASES}."
            exit 1
          fi

          ZEBRAD_RELEASE="$(jq -c 'map(select((.package_name // .package // .name) == "zebrad")) | first // empty' <<< "${RELEASES:-[]}")"

          if [ -z "${ZEBRAD_RELEASE}" ]; then
            if [ "${NOTES_REQUIRED}" = "true" ]; then
              echo "::error::release-plz did not return the required zebrad ${EXPECTED_VERSION} (${EXPECTED_TAG}) release."
              exit 1
            fi

            echo "released=false" >> "${GITHUB_OUTPUT}"
            echo "No zebrad release in release-plz output; skipping GitHub Release."
            exit 0
          fi

          VERSION="$(jq -r '.version' <<< "${ZEBRAD_RELEASE}")"
          TAG="$(jq -r '.tag' <<< "${ZEBRAD_RELEASE}")"

          if [ -z "${VERSION}" ] || [ "${VERSION}" = "null" ] || [ -z "${TAG}" ] || [ "${TAG}" = "null" ]; then
            echo "::error::release-plz did not return both version and tag for zebrad: ${ZEBRAD_RELEASE}"
            exit 1
          fi

          if [ "${NOTES_REQUIRED}" != "true" ] || [ "${VERSION}" != "${EXPECTED_VERSION}" ] || [ "${TAG}" != "${EXPECTED_TAG}" ]; then
            echo "::error::release-plz returned zebrad ${VERSION} (${TAG}), but the validated release notes were for ${EXPECTED_VERSION:-none} (${EXPECTED_TAG:-none})."
            exit 1
          fi

          {
            echo "released=true"
            echo "version=${VERSION}"
            echo "tag=${TAG}"
            if [[ "${VERSION}" == *-* ]]; then
              echo "prerelease=true"
            else
              echo "prerelease=false"
            fi
          } >> "${GITHUB_OUTPUT}"
      - uses: actions-rust-lang/setup-rust-toolchain@46268bd060767258de96ed93c1251119784f2ab6 #v1.16.1
        if: steps.zebrad-release.outputs.released == 'true'
        with:
          toolchain: stable
          cache-on-failure: true
      - name: Verify zebrad installs from crates.io
        if: steps.zebrad-release.outputs.released == 'true'
        timeout-minutes: 30
        env:
          VERSION: ${{ steps.zebrad-release.outputs.version }}
        run: |
          set -euo pipefail

          cargo install --locked --force --version "${VERSION}" zebrad
          ~/.cargo/bin/zebrad --version

      - name: Enhance release notes with Claude
        id: enhance
        if: steps.zebrad-release.outputs.released == 'true' && vars.CLAUDE_ENABLED != ''
        continue-on-error: true
        uses: anthropics/claude-code-action@11ba60486e4aec9ddfeafcf4bb3f00b028ac2c16 # v1.0.142
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Provide a token so the action skips its OIDC exchange, which needs id-token: write.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prompt: |
            Rewrite the changelog section below into operator-facing GitHub Release
            notes for Zebra ${{ steps.zebrad-release.outputs.tag }}.

            Rules:
            - Keep the "## [Zebra <version>]" heading line exactly as given.
            - After the heading, add an "Update priority" line and a one-paragraph
              summary for node operators, keeping any existing summary text.
            - Keep every changelog entry; do not invent, drop, or reorder entries.
            - Do not add detail that is not present in the section.
            Return the full release notes, heading included, in the structured output.

            Changelog section:
            ${{ steps.notes.outputs.section }}
          claude_args: |
            --json-schema '{"type":"object","properties":{"notes":{"type":"string"}},"required":["notes"],"additionalProperties":false}'
            --max-turns 5

      - name: Select release notes
        if: steps.zebrad-release.outputs.released == 'true'
        env:
          STRUCTURED: ${{ steps.enhance.outputs.structured_output }}
        run: |
          set -euo pipefail

          enhanced="${RUNNER_TEMP}/release-notes.enhanced.md"
          final="${RUNNER_TEMP}/release-notes.final.md"

          if [ -n "${STRUCTURED}" ]; then
            jq -r '.notes // empty' <<< "${STRUCTURED}" > "${enhanced}"
          fi

          # Use Claude's notes only when non-empty and still carrying the version
          # heading; otherwise keep the deterministic extracted notes.
          if [ -s "${enhanced}" ] && grep -q '^## \[Zebra ' "${enhanced}"; then
            cp "${enhanced}" "${final}"
            echo "Using Claude-enhanced release notes."
          else
            cp "${RUNNER_TEMP}/release-notes.md" "${final}"
            echo "Using extracted changelog notes."
          fi

      - name: Create zebrad GitHub Release
        if: steps.zebrad-release.outputs.released == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          REPOSITORY: ${{ github.repository }}
          TARGET_SHA: ${{ github.event.pull_request.merge_commit_sha }}
          TAG: ${{ steps.zebrad-release.outputs.tag }}
          PRERELEASE: ${{ steps.zebrad-release.outputs.prerelease }}
        run: |
          set -euo pipefail

          notes_file="${RUNNER_TEMP}/release-notes.final.md"
          release_flags=(--latest --prerelease=false)

          if [ "${PRERELEASE}" = "true" ]; then
            release_flags=(--latest=false --prerelease)
          fi

          if gh release view "${TAG}" --repo "${REPOSITORY}" >/dev/null 2>&1; then
            gh release edit "${TAG}" \
              --repo "${REPOSITORY}" \
              --title "Zebra ${TAG}" \
              --notes-file "${notes_file}" \
              --target "${TARGET_SHA}" \
              "${release_flags[@]}"
          else
            gh release create "${TAG}" \
              --repo "${REPOSITORY}" \
              --title "Zebra ${TAG}" \
              --notes-file "${notes_file}" \
              --target "${TARGET_SHA}" \
              --verify-tag \
              "${release_flags[@]}"
          fi
