steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/$BRANCH_NAME:$SHORT_SHA', '.']

options:
    machineType: 'N1_HIGHCPU_32'

timeout: 1800s # Half hour for all steps.
