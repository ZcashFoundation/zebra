{
  "openrpc": "1.3.2",
  "info": {
    "title": "zebra-rpc",
    "description": "A Zebra JSON Remote Procedure Call (JSON-RPC) interface",
    "version": "3.1.0"
  },
  "methods": [
    {
      "name": "getrawmempool",
      "summary": "Returns all transaction ids in the memory pool, as a JSON array.",
      "description": "Returns all transaction ids in the memory pool, as a JSON array.\n\n# Parameters\n\n- `verbose`: (boolean, optional, default=false) true for a json object, false for array of transaction ids.\n\nzcashd reference: [`getrawmempool`](https://zcash.github.io/rpc/getrawmempool.html)\nmethod: post\ntags: blockchain",
      "params": [
        {
          "name": "verbose",
          "summary": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "description": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "getrawmempool_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "z_getsubtreesbyindex",
      "summary": "Returns information about a range of Sapling or Orchard subtrees.",
      "description": "Returns information about a range of Sapling or Orchard subtrees.\n\nzcashd reference: [`z_getsubtreesbyindex`](https://zcash.github.io/rpc/z_getsubtreesbyindex.html) - TODO: fix link\nmethod: post\ntags: blockchain\n\n# Parameters\n\n- `pool`: (string, required) The pool from which subtrees should be returned. Either \"sapling\" or \"orchard\".\n- `start_index`: (number, required) The index of the first 2^16-leaf subtree to return.\n- `limit`: (number, optional) The maximum number of subtree values to return.\n\n# Notes\n\nWhile Zebra is doing its initial subtree index rebuild, subtrees will become available\nstarting at the chain tip. This RPC will return an empty list if the `start_index` subtree\nexists, but has not been rebuilt yet. This matches `zcashd`'s behaviour when subtrees aren't\navailable yet. (But `zcashd` does its rebuild before syncing any blocks.)",
      "params": [
        {
          "name": "pool",
          "summary": "The pool from which subtrees should be returned. Either \"sapling\" or \"orchard\".",
          "description": "The pool from which subtrees should be returned. Either \"sapling\" or \"orchard\".",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "start_index",
          "summary": "The index of the first 2^16-leaf subtree to return.",
          "description": "The index of the first 2^16-leaf subtree to return.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/NoteCommitmentSubtreeIndex"
          }
        },
        {
          "name": "limit",
          "summary": "The maximum number of subtrees to return.",
          "description": "The maximum number of subtrees to return.",
          "schema": {
            "$ref": "#/components/schemas/NoteCommitmentSubtreeIndex"
          }
        }
      ],
      "result": {
        "name": "z_getsubtreesbyindex_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getbestblockhash",
      "summary": "Returns the hash of the current best blockchain tip block, as a [`GetBlockHash`] JSON string.",
      "description": "Returns the hash of the current best blockchain tip block, as a [`GetBlockHash`] JSON string.\n\nzcashd reference: [`getbestblockhash`](https://zcash.github.io/rpc/getbestblockhash.html)\nmethod: post\ntags: blockchain",
      "params": [],
      "result": {
        "name": "getbestblockhash_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "stop",
      "summary": "Stop the running zebrad process.",
      "description": "Stop the running zebrad process.\n\n# Notes\n\n- Works for non windows targets only.\n- Works only if the network of the running zebrad process is `Regtest`.\n\nzcashd reference: [`stop`](https://zcash.github.io/rpc/stop.html)\nmethod: post\ntags: control",
      "params": [],
      "result": {
        "name": "stop_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getaddresstxids",
      "summary": "Returns the transaction ids made by the provided transparent addresses.",
      "description": "Returns the transaction ids made by the provided transparent addresses.\n\nzcashd reference: [`getaddresstxids`](https://zcash.github.io/rpc/getaddresstxids.html)\nmethod: post\ntags: address\n\n# Parameters\n\n- `request`: (required) Either:\n    - A single address string (e.g., `\"tmYXBYJj1K7vhejSec5osXK2QsGa5MTisUQ\"`), or\n    - An object with the following named fields:\n        - `addresses`: (array of strings, required) The addresses to get transactions from.\n        - `start`: (numeric, optional) The lower height to start looking for transactions (inclusive).\n        - `end`: (numeric, optional) The upper height to stop looking for transactions (inclusive).\n\nExample of the object form:\n```json\n{\n  \"addresses\": [\"tmYXBYJj1K7vhejSec5osXK2QsGa5MTisUQ\"],\n  \"start\": 1000,\n  \"end\": 2000\n}\n```\n\n# Notes\n\n- Only the multi-argument format is used by lightwalletd and this is what we currently support:\n<https://github.com/zcash/lightwalletd/blob/631bb16404e3d8b045e74a7c5489db626790b2f6/common/common.go#L97-L102>\n- It is recommended that users call the method with start/end heights such that the response can't be too large.",
      "params": [
        {
          "name": "request",
          "summary": "The request object containing the parameters.",
          "description": "The request object containing the parameters.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/GetAddressTxIdsRequest"
          }
        }
      ],
      "result": {
        "name": "getaddresstxids_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblockhash",
      "summary": "Returns the hash of the block of a given height iff the index argument correspond",
      "description": "Returns the hash of the block of a given height iff the index argument correspond\nto a block in the best chain.\n\nzcashd reference: [`getblockhash`](https://zcash-rpc.github.io/getblockhash.html)\nmethod: post\ntags: blockchain\n\n# Parameters\n\n- `index`: (numeric, required, example=1) The block index.\n\n# Notes\n\n- If `index` is positive then index = block height.\n- If `index` is negative then -1 is the last known valid block.",
      "params": [
        {
          "name": "index",
          "summary": "The index of the subtree to return.",
          "description": "The index of the subtree to return.",
          "required": true,
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        }
      ],
      "result": {
        "name": "getblockhash_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "sendrawtransaction",
      "summary": "Sends the raw bytes of a signed transaction to the local node's mempool, if the transaction is valid.",
      "description": "Sends the raw bytes of a signed transaction to the local node's mempool, if the transaction is valid.\nReturns the [`SentTransactionHash`] for the transaction, as a JSON string.\n\nzcashd reference: [`sendrawtransaction`](https://zcash.github.io/rpc/sendrawtransaction.html)\nmethod: post\ntags: transaction\n\n# Parameters\n\n- `raw_transaction_hex`: (string, required, example=\"signedhex\") The hex-encoded raw transaction bytes.\n- `allow_high_fees`: (bool, optional) A legacy parameter accepted by zcashd but ignored by Zebra.\n\n# Notes\n\nzcashd accepts an optional `allowhighfees` parameter. Zebra doesn't support this parameter,\nbecause lightwalletd doesn't use it.",
      "params": [
        {
          "name": "raw_transaction_hex",
          "summary": "The hex-encoded raw transaction bytes.",
          "description": "The hex-encoded raw transaction bytes.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "_allow_high_fees",
          "summary": "Whether to allow high fees.",
          "description": "Whether to allow high fees.",
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "sendrawtransaction_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getnetworksolps",
      "summary": "Returns the estimated network solutions per second based on the last `num_blocks` before",
      "description": "Returns the estimated network solutions per second based on the last `num_blocks` before\n`height`.\n\nIf `num_blocks` is not supplied, uses 120 blocks. If it is 0 or -1, uses the difficulty\naveraging window.\nIf `height` is not supplied or is -1, uses the tip height.\n\nzcashd reference: [`getnetworksolps`](https://zcash.github.io/rpc/getnetworksolps.html)\nmethod: post\ntags: mining",
      "params": [
        {
          "name": "num_blocks",
          "summary": "The number of blocks to return.",
          "description": "The number of blocks to return.",
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        },
        {
          "name": "height",
          "summary": "The height of the block to return.",
          "description": "The height of the block to return.",
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        }
      ],
      "result": {
        "name": "getnetworksolps_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "rpc.discover",
      "summary": "Returns an OpenRPC schema as a description of this service.",
      "description": "Returns an OpenRPC schema as a description of this service.",
      "params": [],
      "result": {
        "name": "rpc.discover_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getmininginfo",
      "summary": "Returns mining-related information.",
      "description": "Returns mining-related information.\n\nzcashd reference: [`getmininginfo`](https://zcash.github.io/rpc/getmininginfo.html)\nmethod: post\ntags: mining",
      "params": [],
      "result": {
        "name": "getmininginfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "addnode",
      "summary": "Add or remove a node from the address book.",
      "description": "Add or remove a node from the address book.\n\n# Parameters\n\n- `addr`: (string, required) The address of the node to add or remove.\n- `command`: (string, required) The command to execute, either \"add\", \"onetry\", or \"remove\".\n\n# Notes\n\nOnly the \"add\" command is currently supported.\n\nzcashd reference: [`addnode`](https://zcash.github.io/rpc/addnode.html)\nmethod: post\ntags: network",
      "params": [
        {
          "name": "addr",
          "summary": "The address to return.",
          "description": "The address to return.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/PeerSocketAddr"
          }
        },
        {
          "name": "command",
          "summary": "The command to execute.",
          "description": "The command to execute.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/AddNodeCommand"
          }
        }
      ],
      "result": {
        "name": "addnode_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "submitblock",
      "summary": "Submits block to the node to be validated and committed.",
      "description": "Submits block to the node to be validated and committed.\nReturns the [`SubmitBlockResponse`] for the operation, as a JSON string.\n\nzcashd reference: [`submitblock`](https://zcash.github.io/rpc/submitblock.html)\nmethod: post\ntags: mining\n\n# Parameters\n\n- `hexdata`: (string, required)\n- `jsonparametersobject`: (string, optional) - currently ignored\n\n# Notes\n\n - `jsonparametersobject` holds a single field, workid, that must be included in submissions if provided by the server.",
      "params": [
        {
          "name": "hex_data",
          "summary": "The hex-encoded data to return.",
          "description": "The hex-encoded data to return.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/HexData"
          }
        },
        {
          "name": "_parameters",
          "summary": "The parameters for the command.",
          "description": "The parameters for the command.",
          "schema": {
            "$ref": "#/components/schemas/SubmitBlockParameters"
          }
        }
      ],
      "result": {
        "name": "submitblock_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "reconsiderblock",
      "summary": "Reconsiders a previously invalidated block if it exists in the cache of previously invalidated blocks.",
      "description": "Reconsiders a previously invalidated block if it exists in the cache of previously invalidated blocks.\n\n# Parameters\n\n- `block_hash`: (hex-encoded block hash, required) The block hash to reconsider.",
      "params": [
        {
          "name": "block_hash",
          "summary": "The hash of the block to return.",
          "description": "The hash of the block to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "reconsiderblock_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "validateaddress",
      "summary": "Checks if a zcash transparent address of type P2PKH, P2SH or TEX is valid.",
      "description": "Checks if a zcash transparent address of type P2PKH, P2SH or TEX is valid.\nReturns information about the given address if valid.\n\nzcashd reference: [`validateaddress`](https://zcash.github.io/rpc/validateaddress.html)\nmethod: post\ntags: util\n\n# Parameters\n\n- `address`: (string, required) The zcash address to validate.",
      "params": [
        {
          "name": "address",
          "summary": "The address to return.",
          "description": "The address to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "validateaddress_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getbestblockheightandhash",
      "summary": "Returns the height and hash of the current best blockchain tip block, as a [`GetBlockHeightAndHashResponse`] JSON struct.",
      "description": "Returns the height and hash of the current best blockchain tip block, as a [`GetBlockHeightAndHashResponse`] JSON struct.\n\nzcashd reference: none\nmethod: post\ntags: blockchain",
      "params": [],
      "result": {
        "name": "getbestblockheightandhash_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblockcount",
      "summary": "Returns the height of the most recent block in the best valid block chain (equivalently,",
      "description": "Returns the height of the most recent block in the best valid block chain (equivalently,\nthe number of blocks in this chain excluding the genesis block).\n\nzcashd reference: [`getblockcount`](https://zcash.github.io/rpc/getblockcount.html)\nmethod: post\ntags: blockchain",
      "params": [],
      "result": {
        "name": "getblockcount_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getaddressbalance",
      "summary": "Returns the total balance of a provided `addresses` in an [`AddressBalance`] instance.",
      "description": "Returns the total balance of a provided `addresses` in an [`AddressBalance`] instance.\n\nzcashd reference: [`getaddressbalance`](https://zcash.github.io/rpc/getaddressbalance.html)\nmethod: post\ntags: address\n\n# Parameters\n\n- `address_strings`: (object, example={\"addresses\": [\"tmYXBYJj1K7vhejSec5osXK2QsGa5MTisUQ\"]}) A JSON map with a single entry\n    - `addresses`: (array of strings) A list of base-58 encoded addresses.\n\n# Notes\n\nzcashd also accepts a single string parameter instead of an array of strings, but Zebra\ndoesn't because lightwalletd always calls this RPC with an array of addresses.\n\nzcashd also returns the total amount of Zatoshis received by the addresses, but Zebra\ndoesn't because lightwalletd doesn't use that information.\n\nThe RPC documentation says that the returned object has a string `balance` field, but\nzcashd actually [returns an\ninteger](https://github.com/zcash/lightwalletd/blob/bdaac63f3ee0dbef62bde04f6817a9f90d483b00/common/common.go#L128-L130).",
      "params": [
        {
          "name": "address_strings",
          "summary": "The addresses to return.",
          "description": "The addresses to return.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/GetAddressBalanceRequest"
          }
        }
      ],
      "result": {
        "name": "getaddressbalance_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getinfo",
      "summary": "Returns software information from the RPC server, as a [`GetInfo`] JSON struct.",
      "description": "Returns software information from the RPC server, as a [`GetInfo`] JSON struct.\n\nzcashd reference: [`getinfo`](https://zcash.github.io/rpc/getinfo.html)\nmethod: post\ntags: control\n\n# Notes\n\n[The zcashd reference](https://zcash.github.io/rpc/getinfo.html) might not show some fields\nin Zebra's [`GetInfo`]. Zebra uses the field names and formats from the\n[zcashd code](https://github.com/zcash/zcash/blob/v4.6.0-1/src/rpc/misc.cpp#L86-L87).\n\nSome fields from the zcashd reference are missing from Zebra's [`GetInfo`]. It only contains the fields\n[required for lightwalletd support.](https://github.com/zcash/lightwalletd/blob/v0.4.9/common/common.go#L91-L95)",
      "params": [],
      "result": {
        "name": "getinfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "generate",
      "summary": "Mine blocks immediately. Returns the block hashes of the generated blocks.",
      "description": "Mine blocks immediately. Returns the block hashes of the generated blocks.\n\n# Parameters\n\n- `num_blocks`: (numeric, required, example=1) Number of blocks to be generated.\n\n# Notes\n\nOnly works if the network of the running zebrad process is `Regtest`.\n\nzcashd reference: [`generate`](https://zcash.github.io/rpc/generate.html)\nmethod: post\ntags: generating",
      "params": [
        {
          "name": "num_blocks",
          "summary": "The number of blocks to return.",
          "description": "The number of blocks to return.",
          "required": true,
          "schema": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          }
        }
      ],
      "result": {
        "name": "generate_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getnetworkhashps",
      "summary": "Returns the estimated network solutions per second based on the last `num_blocks` before",
      "description": "Returns the estimated network solutions per second based on the last `num_blocks` before\n`height`.\n\nThis method name is deprecated, use [`getnetworksolps`](Self::get_network_sol_ps) instead.\nSee that method for details.\n\nzcashd reference: [`getnetworkhashps`](https://zcash.github.io/rpc/getnetworkhashps.html)\nmethod: post\ntags: mining",
      "params": [
        {
          "name": "num_blocks",
          "summary": "The number of blocks to return.",
          "description": "The number of blocks to return.",
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        },
        {
          "name": "height",
          "summary": "The height of the block to return.",
          "description": "The height of the block to return.",
          "schema": {
            "type": "integer",
            "format": "int32"
          }
        }
      ],
      "result": {
        "name": "getnetworkhashps_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblockchaininfo",
      "summary": "Returns blockchain state information, as a [`GetBlockchainInfoResponse`] JSON struct.",
      "description": "Returns blockchain state information, as a [`GetBlockchainInfoResponse`] JSON struct.\n\nzcashd reference: [`getblockchaininfo`](https://zcash.github.io/rpc/getblockchaininfo.html)\nmethod: post\ntags: blockchain\n\n# Notes\n\nSome fields from the zcashd reference are missing from Zebra's [`GetBlockchainInfoResponse`]. It only contains the fields\n[required for lightwalletd support.](https://github.com/zcash/lightwalletd/blob/v0.4.9/common/common.go#L72-L89)",
      "params": [],
      "result": {
        "name": "getblockchaininfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getaddressutxos",
      "summary": "Returns all unspent outputs for a list of addresses.",
      "description": "Returns all unspent outputs for a list of addresses.\n\nzcashd reference: [`getaddressutxos`](https://zcash.github.io/rpc/getaddressutxos.html)\nmethod: post\ntags: address\n\n# Parameters\n\n- `request`: (required) Either:\n    - A single address string (e.g., `\"tmYXBYJj1K7vhejSec5osXK2QsGa5MTisUQ\"`), or\n    - An object with the following named fields:\n        - `addresses`: (array, required, example=[\\\"tmYXBYJj1K7vhejSec5osXK2QsGa5MTisUQ\\\"]) The addresses to get outputs from.\n        - `chaininfo`: (boolean, optional, default=false) Include chain info with results\n\n# Notes\n\nlightwalletd always uses the multi-address request, without chaininfo:\n<https://github.com/zcash/lightwalletd/blob/master/frontend/service.go#L402>",
      "params": [
        {
          "name": "request",
          "summary": "The request object containing the parameters.",
          "description": "The request object containing the parameters.",
          "required": true,
          "schema": {
            "$ref": "#/components/schemas/GetAddressUtxosRequest"
          }
        }
      ],
      "result": {
        "name": "getaddressutxos_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "invalidateblock",
      "summary": "Invalidates a block if it is not yet finalized, removing it from the non-finalized",
      "description": "Invalidates a block if it is not yet finalized, removing it from the non-finalized\nstate if it is present and rejecting it during contextual validation if it is submitted.\n\n# Parameters\n\n- `block_hash`: (hex-encoded block hash, required) The block hash to invalidate.",
      "params": [
        {
          "name": "block_hash",
          "summary": "The hash of the block to return.",
          "description": "The hash of the block to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "invalidateblock_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblocksubsidy",
      "summary": "Returns the block subsidy reward of the block at `height`, taking into account the mining slow start.",
      "description": "Returns the block subsidy reward of the block at `height`, taking into account the mining slow start.\nReturns an error if `height` is less than the height of the first halving for the current network.\n\nzcashd reference: [`getblocksubsidy`](https://zcash.github.io/rpc/getblocksubsidy.html)\nmethod: post\ntags: mining\n\n# Parameters\n\n- `height`: (numeric, optional, example=1) Can be any valid current or future height.\n\n# Notes\n\nIf `height` is not supplied, uses the tip height.",
      "params": [
        {
          "name": "height",
          "summary": "The height of the block to return.",
          "description": "The height of the block to return.",
          "schema": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          }
        }
      ],
      "result": {
        "name": "getblocksubsidy_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getrawtransaction",
      "summary": "Returns the raw transaction data, as a [`GetRawTransaction`] JSON string or structure.",
      "description": "Returns the raw transaction data, as a [`GetRawTransaction`] JSON string or structure.\n\nzcashd reference: [`getrawtransaction`](https://zcash.github.io/rpc/getrawtransaction.html)\nmethod: post\ntags: transaction\n\n# Parameters\n\n- `txid`: (string, required, example=\"mytxid\") The transaction ID of the transaction to be returned.\n- `verbose`: (number, optional, default=0, example=1) If 0, return a string of hex-encoded data, otherwise return a JSON object.\n- `blockhash` (string, optional) The block in which to look for the transaction",
      "params": [
        {
          "name": "txid",
          "summary": "The transaction ID to return.",
          "description": "The transaction ID to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "verbose",
          "summary": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "description": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "schema": {
            "type": "integer",
            "format": "uint8",
            "maximum": 255,
            "minimum": 0
          }
        },
        {
          "name": "block_hash",
          "summary": "The hash of the block to return.",
          "description": "The hash of the block to return.",
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "getrawtransaction_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "z_validateaddress",
      "summary": "Checks if a zcash address of type P2PKH, P2SH, TEX, SAPLING or UNIFIED is valid.",
      "description": "Checks if a zcash address of type P2PKH, P2SH, TEX, SAPLING or UNIFIED is valid.\nReturns information about the given address if valid.\n\nzcashd reference: [`z_validateaddress`](https://zcash.github.io/rpc/z_validateaddress.html)\nmethod: post\ntags: util\n\n# Parameters\n\n- `address`: (string, required) The zcash address to validate.\n\n# Notes\n\n- No notes",
      "params": [
        {
          "name": "address",
          "summary": "The address to return.",
          "description": "The address to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "z_validateaddress_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getdifficulty",
      "summary": "Returns the proof-of-work difficulty as a multiple of the minimum difficulty.",
      "description": "Returns the proof-of-work difficulty as a multiple of the minimum difficulty.\n\nzcashd reference: [`getdifficulty`](https://zcash.github.io/rpc/getdifficulty.html)\nmethod: post\ntags: blockchain",
      "params": [],
      "result": {
        "name": "getdifficulty_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getpeerinfo",
      "summary": "Returns data about each connected network node.",
      "description": "Returns data about each connected network node.\n\nzcashd reference: [`getpeerinfo`](https://zcash.github.io/rpc/getpeerinfo.html)\nmethod: post\ntags: network",
      "params": [],
      "result": {
        "name": "getpeerinfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getnetworkinfo",
      "summary": "Returns an object containing various state info regarding P2P networking.",
      "description": "Returns an object containing various state info regarding P2P networking.\n\nzcashd reference: [`getnetworkinfo`](https://zcash.github.io/rpc/getnetworkinfo.html)\nmethod: post\ntags: network",
      "params": [],
      "result": {
        "name": "getnetworkinfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "z_listunifiedreceivers",
      "summary": "Returns the list of individual payment addresses given a unified address.",
      "description": "Returns the list of individual payment addresses given a unified address.\n\nzcashd reference: [`z_listunifiedreceivers`](https://zcash.github.io/rpc/z_listunifiedreceivers.html)\nmethod: post\ntags: wallet\n\n# Parameters\n\n- `address`: (string, required) The zcash unified address to get the list from.\n\n# Notes\n\n- No notes",
      "params": [
        {
          "name": "address",
          "summary": "The address to return.",
          "description": "The address to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "z_listunifiedreceivers_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "z_gettreestate",
      "summary": "Returns information about the given block's Sapling & Orchard tree state.",
      "description": "Returns information about the given block's Sapling & Orchard tree state.\n\nzcashd reference: [`z_gettreestate`](https://zcash.github.io/rpc/z_gettreestate.html)\nmethod: post\ntags: blockchain\n\n# Parameters\n\n- `hash | height`: (string, required, example=\"00000000febc373a1da2bd9f887b105ad79ddc26ac26c2b28652d64e5207c5b5\") The block hash or height.\n\n# Notes\n\nThe zcashd doc reference above says that the parameter \"`height` can be\nnegative where -1 is the last known valid block\". On the other hand,\n`lightwalletd` only uses positive heights, so Zebra does not support\nnegative heights.",
      "params": [
        {
          "name": "hash_or_height",
          "summary": "The block hash or height to return.",
          "description": "The block hash or height to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        }
      ],
      "result": {
        "name": "z_gettreestate_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblocktemplate",
      "summary": "Returns a block template for mining new Zcash blocks.",
      "description": "Returns a block template for mining new Zcash blocks.\n\n# Parameters\n\n- `jsonrequestobject`: (string, optional) A JSON object containing arguments.\n\nzcashd reference: [`getblocktemplate`](https://zcash-rpc.github.io/getblocktemplate.html)\nmethod: post\ntags: mining\n\n# Notes\n\nArguments to this RPC are currently ignored.\nLong polling, block proposals, server lists, and work IDs are not supported.\n\nMiners can make arbitrary changes to blocks, as long as:\n- the data sent to `submitblock` is a valid Zcash block, and\n- the parent block is a valid block that Zebra already has, or will receive soon.\n\nZebra verifies blocks in parallel, and keeps recent chains in parallel,\nso moving between chains and forking chains is very cheap.",
      "params": [
        {
          "name": "parameters",
          "summary": "The parameters for the command.",
          "description": "The parameters for the command.",
          "schema": {
            "$ref": "#/components/schemas/GetBlockTemplateParameters"
          }
        }
      ],
      "result": {
        "name": "getblocktemplate_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblock",
      "summary": "Returns the requested block by hash or height, as a [`GetBlock`] JSON string.",
      "description": "Returns the requested block by hash or height, as a [`GetBlock`] JSON string.\nIf the block is not in Zebra's state, returns\n[error code `-8`.](https://github.com/zcash/zcash/issues/5758) if a height was\npassed or -5 if a hash was passed.\n\nzcashd reference: [`getblock`](https://zcash.github.io/rpc/getblock.html)\nmethod: post\ntags: blockchain\n\n# Parameters\n\n- `hash_or_height`: (string, required, example=\"1\") The hash or height for the block to be returned.\n- `verbosity`: (number, optional, default=1, example=1) 0 for hex encoded data, 1 for a json object, and 2 for json object with transaction data.\n\n# Notes\n\nThe `size` field is only returned with verbosity=2.\n\nThe undocumented `chainwork` field is not returned.",
      "params": [
        {
          "name": "hash_or_height",
          "summary": "The block hash or height to return.",
          "description": "The block hash or height to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "verbosity",
          "summary": "Whether to include verbose output.",
          "description": "Whether to include verbose output.",
          "schema": {
            "type": "integer",
            "format": "uint8",
            "maximum": 255,
            "minimum": 0
          }
        }
      ],
      "result": {
        "name": "getblock_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getblockheader",
      "summary": "Returns the requested block header by hash or height, as a [`GetBlockHeader`] JSON string.",
      "description": "Returns the requested block header by hash or height, as a [`GetBlockHeader`] JSON string.\nIf the block is not in Zebra's state,\nreturns [error code `-8`.](https://github.com/zcash/zcash/issues/5758)\nif a height was passed or -5 if a hash was passed.\n\nzcashd reference: [`getblockheader`](https://zcash.github.io/rpc/getblockheader.html)\nmethod: post\ntags: blockchain\n\n# Parameters\n\n- `hash_or_height`: (string, required, example=\"1\") The hash or height for the block to be returned.\n- `verbose`: (bool, optional, default=false, example=true) false for hex encoded data, true for a json object\n\n# Notes\n\nThe undocumented `chainwork` field is not returned.",
      "params": [
        {
          "name": "hash_or_height",
          "summary": "The block hash or height to return.",
          "description": "The block hash or height to return.",
          "required": true,
          "schema": {
            "type": "string"
          }
        },
        {
          "name": "verbose",
          "summary": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "description": "Boolean flag to indicate verbosity, true for a json object, false for hex encoded data.",
          "schema": {
            "type": "boolean"
          }
        }
      ],
      "result": {
        "name": "getblockheader_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    },
    {
      "name": "getmempoolinfo",
      "summary": "Returns details on the active state of the TX memory pool.",
      "description": "Returns details on the active state of the TX memory pool.\n\nzcash reference: [`getmempoolinfo`](https://zcash.github.io/rpc/getmempoolinfo.html)",
      "params": [],
      "result": {
        "name": "getmempoolinfo_result",
        "summary": "An OpenRPC document.",
        "description": "An OpenRPC document.",
        "schema": {
          "$ref": "#/components/schemas/OpenRPC%20Schema"
        }
      }
    }
  ],
  "components": {
    "schemas": {
      "AddNodeCommand": {
        "description": "Commands for the `addnode` RPC method.",
        "oneOf": [
          {
            "const": "add",
            "description": "Add a node to the address book.",
            "type": "string"
          }
        ]
      },
      "GetAddressBalanceRequest": {
        "anyOf": [
          {
            "description": "A list of address strings.",
            "properties": {
              "addresses": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "addresses"
            ],
            "type": "object"
          },
          {
            "description": "A single address string.",
            "type": "string"
          }
        ],
        "description": "A request for [`RpcServer::get_address_balance`]."
      },
      "GetAddressTxIdsRequest": {
        "anyOf": [
          {
            "description": "A single address string.",
            "type": "string"
          },
          {
            "description": "A full request object with address list and optional height range.",
            "properties": {
              "addresses": {
                "description": "A list of addresses to get transactions from.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "end": {
                "description": "The height to end looking for transactions.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "start": {
                "description": "The height to start looking for transactions.",
                "format": "uint32",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              }
            },
            "required": [
              "addresses"
            ],
            "type": "object"
          }
        ],
        "description": "Parameters of [`RpcServer::get_address_tx_ids`] RPC method.\n\nSee [`RpcServer::get_address_tx_ids`] for more details."
      },
      "GetAddressUtxosRequest": {
        "anyOf": [
          {
            "description": "A single address string.",
            "type": "string"
          },
          {
            "description": "A full request object with address list and chainInfo flag.",
            "properties": {
              "addresses": {
                "description": "A list of addresses to get transactions from.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "chainInfo": {
                "default": false,
                "description": "The height to start looking for transactions.",
                "type": "boolean"
              }
            },
            "required": [
              "addresses"
            ],
            "type": "object"
          }
        ],
        "description": "Parameters of [`RpcServer::get_address_utxos`] RPC method."
      },
      "GetBlockTemplateCapability": {
        "description": "Valid `capabilities` values that indicate client-side support.",
        "oneOf": [
          {
            "const": "longpoll",
            "description": "Long Polling support.\nCurrently ignored by zebra.",
            "type": "string"
          },
          {
            "const": "coinbasetxn",
            "description": "Information for coinbase transaction, default template data with the `coinbasetxn` field.\nCurrently ignored by zebra.",
            "type": "string"
          },
          {
            "const": "coinbasevalue",
            "description": "Coinbase value, template response provides a `coinbasevalue` field and omits `coinbasetxn` field.\nCurrently ignored by zebra.",
            "type": "string"
          },
          {
            "const": "coinbaseaux",
            "description": "Components of the coinbase transaction.\nCurrently ignored by zebra.",
            "type": "string"
          },
          {
            "const": "proposal",
            "description": "Currently ignored by zcashd and zebra.",
            "type": "string"
          },
          {
            "const": "serverlist",
            "description": "Currently ignored by zcashd and zebra.",
            "type": "string"
          },
          {
            "const": "workid",
            "description": "Currently ignored by zcashd and zebra.",
            "type": "string"
          },
          {
            "const": "unknowncapability",
            "description": "Unknown capability to fill in for mutations.",
            "type": "string"
          }
        ]
      },
      "GetBlockTemplateParameters": {
        "description": "Optional parameter `jsonrequestobject` for `getblocktemplate` RPC request.\n\nThe `data` field must be provided in `proposal` mode, and must be omitted in `template` mode.\nAll other fields are optional.",
        "properties": {
          "capabilities": {
            "description": "A list of client-side supported capability features",
            "items": {
              "$ref": "#/components/schemas/GetBlockTemplateCapability"
            },
            "type": "array"
          },
          "data": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/HexData"
              },
              {
                "type": "null"
              }
            ],
            "description": "Must be omitted when `getblocktemplate` RPC is called in \"template\" mode (or when `mode` is omitted).\nMust be provided when `getblocktemplate` RPC is called in \"proposal\" mode.\n\nHex-encoded block data to be validated and checked against the server's usual acceptance rules\n(excluding the check for a valid proof-of-work)."
          },
          "longpollid": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/LongPollId"
              },
              {
                "type": "null"
              }
            ],
            "description": "An ID that delays the RPC response until the template changes.\n\nIn Zebra, the ID represents the chain tip, max time, and mempool contents."
          },
          "mode": {
            "$ref": "#/components/schemas/GetBlockTemplateRequestMode",
            "default": "template",
            "description": "Defines whether the RPC method should generate a block template or attempt to\nvalidate block data, checking against all of the server's usual acceptance rules\n(excluding the check for a valid proof-of-work)."
          },
          "workid": {
            "description": "The workid for the block template.\n\ncurrently unused.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "GetBlockTemplateRequestMode": {
        "description": "Defines whether the RPC method should generate a block template or attempt to validate a block\nproposal.",
        "oneOf": [
          {
            "const": "template",
            "description": "Indicates a request for a block template.",
            "type": "string"
          },
          {
            "const": "proposal",
            "description": "Indicates a request to validate block data.",
            "type": "string"
          }
        ]
      },
      "HexData": {
        "description": "Deserialize hex-encoded strings to bytes.",
        "type": "string"
      },
      "LongPollId": {
        "description": "The encoded long poll ID, generated from the [`LongPollInput`].\n\n`zcashd` IDs are currently 69 hex/decimal digits long.\nSince Zebra's IDs are only 46 hex/decimal digits, mining pools should be able to handle them.",
        "type": "string"
      },
      "NoteCommitmentSubtreeIndex": {
        "description": "A note commitment subtree index, used to identify a subtree in a shielded pool.\nAlso used to count subtrees.",
        "format": "uint16",
        "maximum": 65535,
        "minimum": 0,
        "type": "integer"
      },
      "OpenRPC Schema": {
        "$ref": "https://raw.githubusercontent.com/open-rpc/meta-schema/master/schema.json"
      },
      "PeerSocketAddr": {
        "description": "A thin wrapper for [`SocketAddr`] which hides peer IP addresses in logs and metrics.",
        "type": "string"
      },
      "SubmitBlockParameters": {
        "description": "Optional argument `jsonparametersobject` for `submitblock` RPC request\n\nSee the notes for the [`submit_block`](crate::methods::RpcServer::submit_block) RPC.",
        "properties": {
          "workid": {
            "description": "The workid for the block template. Currently unused.\n\n> If the server provided a workid, it MUST be included with submissions,\n\nRationale:\n\n> If servers allow all mutations, it may be hard to identify which job it is based on.\n> While it may be possible to verify the submission by its content, it is much easier\n> to compare it to the job issued. It is very easy for the miner to keep track of this.\n> Therefore, using a \"workid\" is a very cheap solution to enable more mutations.\n\n<https://en.bitcoin.it/wiki/BIP_0022#Rationale>",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "type": "object"
      }
    }
  }
}